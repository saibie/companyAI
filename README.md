# 🏢 로컬 계층형 AI 기업 (Local Hierarchical AI Corp)

> **로컬 우선, 비용 0원 AI 조직 시뮬레이터**
> **Ollama**, **Django**, **LangGraph**를 사용하여 로컬 환경에서 나만의 AI 에이전트 조직을 구축하고 관리하세요.

## 📖 개요 (Overview)

**Local Hierarchical AI Corp**는 사용자가 인간 CEO가 되어 재귀적인 구조를 가진 AI 에이전트들을 관리하는 시뮬레이션 플랫폼입니다.

단순한 멀티 에이전트 시스템과 달리, 이 프로젝트는 **지휘 통제(Command & Control)** 계층 구조와 **사용자별 독립성(Multi-tenancy)**을 강조합니다:

* **CEO (사용자):** 로그인한 사용자마다 독립된 '가상 회사'를 운영합니다. 고차원적인 목표를 정의하고 결과를 승인/반려합니다.
* **관리자 (Managers):** 업무를 분해하고 하위 직원에게 위임합니다.
* **직원 (Subordinates):** 원자 단위의 작업(검색, 코딩, 계획)을 수행하거나 이슈를 보고합니다.
* **지식 (Wiki):** 태스크가 성공적으로 완료되면, 그 결과는 자동으로 **사내 위키(Corporate Wiki)**에 저장되어 조직의 자산이 됩니다.

모든 AI 추론은 **Ollama**를 통해 로컬에서 수행되므로, 데이터 프라이버시가 보장되며 API 비용이 전혀 들지 않습니다.

---

## ✨ 핵심 기능 (Key Features)

### 🧠 지능형 에이전트 워크플로
* **계층적 로직:** 에이전트는 자신의 하위 직원을 고용(`create_sub_agent`), 해고(`fire_sub_agent`)하거나 업무를 위임(`assign_task`)할 수 있습니다.
* **네이티브 도구 호출:** **LangGraph**와 Ollama의 Tool Calling API를 사용하여 정확도를 높였습니다.
* **재귀적 위임:** 업무는 위에서 아래로 분해되고, 결과는 아래에서 위로 보고됩니다.

### 📚 전사적 지식 관리 (Corporate Memory)
* **자동 아카이빙:** 에이전트가 업무를 완수(Done)하면, 결과물이 자동으로 벡터 DB에 임베딩되어 **위키(Wiki)**에 저장됩니다.
* **지식 검색:** 에이전트는 업무를 수행하기 전, 과거의 성공 사례나 매뉴얼(SOP)을 위키에서 검색하여 참고합니다.

### 🔐 사용자 격리 및 보안 (User Ownership)
* **멀티 테넌시:** 모든 에이전트, 태스크, 위키 데이터는 **로그인한 사용자(Owner)**에게 철저히 격리되어 귀속됩니다.
* **UUID 식별자:** 모든 데이터 모델에 UUIDv4를 적용하여 보안성과 확장성을 강화했습니다.

### 👁️ 휴먼 인 더 루프 (Human-in-the-Loop) 대시보드
* **재귀적 조직도:** 회사 전체의 계층 구조(CEO -> 관리자 -> 직원)를 트리 형태로 한눈에 시각화합니다.
* **업무 승인 시스템:** 에이전트의 산출물을 검토하고 승인/반려(피드백) 할 수 있습니다.

---

## 🛠️ 기술 스택 (Tech Stack)

* **Backend:** Django 5.x (Future-proof for 6.0), Python 3.12
* **AI Engine:** **Ollama** (Llama 3.1, Mistral, Qwen 등 로컬 모델)
* **Orchestration:** **LangGraph** (Stateful Multi-agent Workflow)
* **Database:** PostgreSQL 16 + **pgvector** (Vector Store & RAG)
* **Frontend:** Django Templates + **HTMX** (Dynamic SPA Experience)
* **Infrastructure:** Docker Compose

---

## 🚀 시작하기 (Getting Started)

### 사전 요구사항
1.  **Docker & Docker Compose** 설치
2.  **Ollama** 설치 및 실행 중일 것 (호스트 머신)

### 설치 방법

1.  **저장소 클론:**
    ```bash
    git clone [https://github.com/your-username/local-ai-corp.git](https://github.com/your-username/local-ai-corp.git)
    cd local-ai-corp
    ```

2.  **환경 설정:**
    * `docker-compose.yml`을 열어 `OLLAMA_HOST`를 현재 머신의 IP 주소로 설정합니다.
      (예: `http://host.docker.internal:11434` 또는 실제 LAN IP)

3.  **Docker 실행:**
    ```bash
    docker-compose up --build -d
    ```

4.  **관리자 계정 생성 (필수):**
    * 로그인 기반 시스템이므로 계정이 반드시 필요합니다.
    ```bash
    docker exec -it ai_corp_web python manage.py createsuperuser
    ```

5.  **접속 및 로그인:**
    * 브라우저에서 `http://localhost:8000/` 으로 이동합니다.
    * 생성한 계정으로 **로그인**하면 대시보드로 이동합니다.

---

## 🕹️ 사용 가이드

1.  **첫 번째 매니저 고용:** 대시보드에서 [Create New Agent]를 통해 CEO 직속(Manager=None) 에이전트를 생성합니다.
2.  **업무 할당:** [Create New Task]를 통해 에이전트에게 업무를 지시합니다. (예: "경쟁사 AI 동향 조사해줘")
3.  **관전 및 승인:** 백그라운드에서 에이전트가 생각하고 하위 에이전트를 고용하는 것을 지켜봅니다. 완료된 태스크는 [Tasks Waiting Approval]에서 승인합니다.
4.  **위키 확인:** 승인된 태스크의 결과가 [Company Wiki] 탭에 저장되었는지 확인합니다.

---

## 🛡️ 라이선스

This project is open-source and available under the MIT License.